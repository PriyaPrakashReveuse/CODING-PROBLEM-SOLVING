#include <iostream>
using namespace std;

int main()
{
    int i,n,arr[100];
    cin>>n;
    for(i=0;i<n;i++)
    {
        cin>>arr[i];
    }
    int count=1,j=0;
    for(i=0;i<n;i++)
    {
        if(arr[j]>arr[i+1])
        {
            continue;
        }
        else
        {
            j=i+1;
            count++;
        }
    } 
    cout<<count;
    return 0;
}
